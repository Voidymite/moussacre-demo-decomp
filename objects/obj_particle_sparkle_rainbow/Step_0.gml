if (image_xscale <= 0)
    instance_destroy()
image_angle += (rot * 60)
if (room != rm_wow)
{
    image_xscale -= 0.1
    image_yscale -= 0.1
}
else
{
    image_xscale -= 0.05
    image_yscale -= 0.05
}
x += lengthdir_x(spd, dir)
y += lengthdir_y(spd, dir)
